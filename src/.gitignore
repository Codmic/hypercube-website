# Composer
.composer
vendor/*
!*/vendor/*

# Sass
.sass-cache

# Grav Specific
backup/*
!backup/.*
cache/*
!cache/.*
assets/*
!assets/.*
logs/*
!logs/.*
images/*
!images/.*
user/accounts/*
!user/accounts/.*
user/data/*
!user/data/.*
user/plugins/*
!user/plugins/.*
user/themes/*
!user/themes/.*
# learn2 ships from GPM like every other theme (not tracked here), but this
# one partial has a local bugfix (double <h1> on every docs.md page — the
# partial printed page.title as an h1 on top of the content's own leading
# `# Title`, plus wrapped block-level content in an invalid <p>) that needs
# to survive a theme reinstall until it's reported/fixed upstream. Each
# level below has to be un-ignored-then-reignored in turn — negating a
# whole ancestor directory (rather than just re-excluding its other
# contents) exposes every sibling in it, not just the one file wanted here.
!user/themes/learn2
user/themes/learn2/*
!user/themes/learn2/templates
user/themes/learn2/templates/*
!user/themes/learn2/templates/partials
user/themes/learn2/templates/partials/*
!user/themes/learn2/templates/partials/page.html.twig
# security.yaml holds only policy now (twig_content, twig_sandbox, xss rules) —
# the per-site secret moved to security-private.php in 2.0.0-beta.2
# (GHSA-3f29-pqwf-v4j4), so the policy file is safe (and useful) to commit.
# The *-private.php glob also covers per-plugin secret files written into
# user/config (e.g. api-private.php from the API plugin's JWT secret).
user/**/config/*-private.php
user/**/config/*-private.php.tmp

# Environments
.env
.env.local
.env.*.local
.gravenv

# OS Generated
.DS_Store*
ehthumbs.db
Icon?
Thumbs.db
*.swp

# phpstorm
.idea/*

/user/config/versions.yaml
/user/data/recovery.window
tmp/*
/AGENTS.md
/.claude

# Grav core distribution — this is the getgrav/grav project itself, not
# site content. Restored by bin/setup-grav.sh (pinned version), not tracked.
/bin/
/system/
/tests/
/.phan/
/webserver-configs/
/CHANGELOG.md
/CODE_OF_CONDUCT.md
/CONTRIBUTING.md
/LICENSE.txt
/README.md
/SECURITY.md
/codeception.yml
/composer.json
/composer.lock
/index.php
/now.json
/robots.txt
/.htaccess
/.editorconfig
/.dependencies
/.env.example
/.github/workflows/build.yaml
/.github/workflows/tests.yaml
/.github/workflows/trigger-docker.yml
/.github/workflows/trigger-skeletons.yml
/.github/dependabot.yaml
/.github/FUNDING.yml

# PHPDoc tools
tools/phpdoc/vendor/
tools/phpdoc/output/
/.phpactor.json
